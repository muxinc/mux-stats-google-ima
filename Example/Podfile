use_frameworks!

platform :ios, '12.0'

target 'DemoApp' do
  pod 'Mux-Stats-Google-IMA', :path => '../'
end

target 'MUXSDKImaListener_Tests' do
  pod 'Mux-Stats-Google-IMA', :path => '../'
  pod 'Specta'
    pod 'Expecta'
end
